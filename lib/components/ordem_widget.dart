import 'package:flutter/material.dart';

class OrdemWidget extends StatelessWidget {
  const OrdemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
